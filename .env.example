# .env の雛形。実際の値を書いた .env を作成してください（.env は git 管理外）。
# cp .env.example .env

# スクレイピング時の連絡先メール（User-Agent に含めます）
CONTACT_EMAIL=forbusi2020@gmail.com

# リクエスト間隔の調整（秒）。基本間隔とランダムなゆらぎ。
REQUEST_INTERVAL_SEC=1.5
REQUEST_JITTER_SEC=0.8

# キャッシュ有効期間（時間）
CACHE_TTL_HOURS=24
